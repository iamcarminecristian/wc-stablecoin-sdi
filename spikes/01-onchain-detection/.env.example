# Endpoint RPC. In locale (make init + make demo): http://localhost:8545
# con TOKEN_ADDRESS stampato da "make demo". Su testnet reale usare la
# risposta di GET /tokens dell'API Monerium (vedi spike 2), che restituisce
# gli indirizzi dei contratti per ogni rete supportata.
RPC_URL=
TOKEN_ADDRESS=
WATCH_ADDRESS=
TOKEN_DECIMALS=18
CONFIRMATIONS=12
POLL_MS=5000
